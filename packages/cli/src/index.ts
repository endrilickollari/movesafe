import { CORE_VERSION } from '@movesafe/core';

// Placeholder CLI entry. The real `mv`/`check` commands land in later
// Sprint 1-4 issues; this just proves the core -> cli wiring works end to end.
console.log(`movesafe CLI (core ${CORE_VERSION})`);
